import React from "react";
import FaqList from "./components/FaqList";

const Page = () => {
  return (
    <div>
      <FaqList />
      <div>
        {/* add background assets (stars layer in figma) here if not in layout. */}
      </div>
    </div>
  );
};

export default Page;
